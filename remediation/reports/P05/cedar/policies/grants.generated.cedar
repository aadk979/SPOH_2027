// GENERATED by tools/generate-grants.mjs from schema.cedarschema. Do not edit.
//
// Role policies (ADR-005 §3, layer 1): one permit per Editable action, granted when the
// principal's per-event Role lists the action in `grants` (RolePermission rows).
@id("grant.Announcement.SendEvent")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Announcement.SendEvent",
  resource
)
when { principal.role.grants.contains("Announcement.SendEvent") };

@id("grant.Announcement.SendStation")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Announcement.SendStation",
  resource
)
when { principal.role.grants.contains("Announcement.SendStation") };

@id("grant.Audit.Read")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Audit.Read",
  resource
)
when { principal.role.grants.contains("Audit.Read") };

@id("grant.Card.GenerateBatch")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Card.GenerateBatch",
  resource
)
when { principal.role.grants.contains("Card.GenerateBatch") };

@id("grant.Card.Reissue")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Card.Reissue",
  resource
)
when { principal.role.grants.contains("Card.Reissue") };

@id("grant.Card.Stamp")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Card.Stamp",
  resource
)
when { principal.role.grants.contains("Card.Stamp") };

@id("grant.Card.Void")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Card.Void",
  resource
)
when { principal.role.grants.contains("Card.Void") };

@id("grant.Content.Edit")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Content.Edit",
  resource
)
when { principal.role.grants.contains("Content.Edit") };

@id("grant.Content.Publish")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Content.Publish",
  resource
)
when { principal.role.grants.contains("Content.Publish") };

@id("grant.Count.Adjust")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Count.Adjust",
  resource
)
when { principal.role.grants.contains("Count.Adjust") };

@id("grant.Dashboard.ReadEvent")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Dashboard.ReadEvent",
  resource
)
when { principal.role.grants.contains("Dashboard.ReadEvent") };

@id("grant.Dashboard.ReadStation")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Dashboard.ReadStation",
  resource
)
when { principal.role.grants.contains("Dashboard.ReadStation") };

@id("grant.Event.Close")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Event.Close",
  resource
)
when { principal.role.grants.contains("Event.Close") };

@id("grant.Event.GoLive")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Event.GoLive",
  resource
)
when { principal.role.grants.contains("Event.GoLive") };

@id("grant.Event.MarkReady")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Event.MarkReady",
  resource
)
when { principal.role.grants.contains("Event.MarkReady") };

@id("grant.Event.Rehearse")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Event.Rehearse",
  resource
)
when { principal.role.grants.contains("Event.Rehearse") };

@id("grant.Fallback.Declare")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Fallback.Declare",
  resource
)
when { principal.role.grants.contains("Fallback.Declare") };

@id("grant.Fallback.Import")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Fallback.Import",
  resource
)
when { principal.role.grants.contains("Fallback.Import") };

@id("grant.Footfall.Create")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Footfall.Create",
  resource
)
when { principal.role.grants.contains("Footfall.Create") };

@id("grant.Gift.Redeem")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Gift.Redeem",
  resource
)
when { principal.role.grants.contains("Gift.Redeem") };

@id("grant.Incident.Read")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Incident.Read",
  resource
)
when { principal.role.grants.contains("Incident.Read") };

@id("grant.Incident.Report")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Incident.Report",
  resource
)
when { principal.role.grants.contains("Incident.Report") };

@id("grant.Incident.Update")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Incident.Update",
  resource
)
when { principal.role.grants.contains("Incident.Update") };

@id("grant.LostFound.Claim")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"LostFound.Claim",
  resource
)
when { principal.role.grants.contains("LostFound.Claim") };

@id("grant.LostFound.CloseOut")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"LostFound.CloseOut",
  resource
)
when { principal.role.grants.contains("LostFound.CloseOut") };

@id("grant.LostFound.Log")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"LostFound.Log",
  resource
)
when { principal.role.grants.contains("LostFound.Log") };

@id("grant.LostPerson.Raise")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"LostPerson.Raise",
  resource
)
when { principal.role.grants.contains("LostPerson.Raise") };

@id("grant.LostPerson.Resolve")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"LostPerson.Resolve",
  resource
)
when { principal.role.grants.contains("LostPerson.Resolve") };

@id("grant.People.AssignRole")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"People.AssignRole",
  resource
)
when { principal.role.grants.contains("People.AssignRole") };

@id("grant.People.Deactivate")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"People.Deactivate",
  resource
)
when { principal.role.grants.contains("People.Deactivate") };

@id("grant.People.Invite")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"People.Invite",
  resource
)
when { principal.role.grants.contains("People.Invite") };

@id("grant.People.Read")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"People.Read",
  resource
)
when { principal.role.grants.contains("People.Read") };

@id("grant.People.Update")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"People.Update",
  resource
)
when { principal.role.grants.contains("People.Update") };

@id("grant.Record.Void")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Record.Void",
  resource
)
when { principal.role.grants.contains("Record.Void") };

@id("grant.Registration.Create")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Registration.Create",
  resource
)
when { principal.role.grants.contains("Registration.Create") };

@id("grant.Report.Export")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Report.Export",
  resource
)
when { principal.role.grants.contains("Report.Export") };

@id("grant.Report.Generate")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Report.Generate",
  resource
)
when { principal.role.grants.contains("Report.Generate") };

@id("grant.Roster.Edit")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Roster.Edit",
  resource
)
when { principal.role.grants.contains("Roster.Edit") };

@id("grant.Roster.ReadStation")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Roster.ReadStation",
  resource
)
when { principal.role.grants.contains("Roster.ReadStation") };

@id("grant.Schedule.Manage")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Schedule.Manage",
  resource
)
when { principal.role.grants.contains("Schedule.Manage") };

@id("grant.Settings.ManageEvent")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Settings.ManageEvent",
  resource
)
when { principal.role.grants.contains("Settings.ManageEvent") };

@id("grant.Structure.Change")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Structure.Change",
  resource
)
when { principal.role.grants.contains("Structure.Change") };

@id("grant.Structure.Edit")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Structure.Edit",
  resource
)
when { principal.role.grants.contains("Structure.Edit") };

@id("grant.Structure.Read")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Structure.Read",
  resource
)
when { principal.role.grants.contains("Structure.Read") };

@id("grant.Swap.Decide")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"Swap.Decide",
  resource
)
when { principal.role.grants.contains("Swap.Decide") };

@id("grant.VisitorRecord.Read")
permit (
  principal is SPOH::Membership,
  action == SPOH::Action::"VisitorRecord.Read",
  resource
)
when { principal.role.grants.contains("VisitorRecord.Read") };
