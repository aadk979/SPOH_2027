/**
 * The missionCard module's public API: the only file another module may import
 * (engineering-standards §3). P06.5 fills it in as the module is split.
 */
export { normaliseShortCode } from './shortCode.js';
