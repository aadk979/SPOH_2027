# SPOH 2027 client configuration.
#
# NEXT_PUBLIC_* values are inlined into the browser bundle at build time, so
# nothing secret belongs here. Copy to client/.env.local for local development.

NEXT_PUBLIC_API_BASE_URL=http://localhost:4010
NEXT_PUBLIC_ENV_LABEL=development

# Leave the pool and client blank to keep the development sign-in screen.
# Filling them in switches the client to the Cognito flow; the staging pool
# below is provisioned in ap-southeast-1 and matches the server's .env.example.
#
#   NEXT_PUBLIC_COGNITO_USER_POOL_ID=ap-southeast-1_9bwl2nGF7
#   NEXT_PUBLIC_COGNITO_CLIENT_ID=23uft7mvtnrno1uunsc5lp0h2v
NEXT_PUBLIC_COGNITO_REGION=ap-southeast-1
NEXT_PUBLIC_COGNITO_USER_POOL_ID=
NEXT_PUBLIC_COGNITO_CLIENT_ID=
